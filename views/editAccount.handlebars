<form id="registrationForm" method="post" action="/api/users?_method=PATCH">
  <div id="textInputs">
    <label>
      First Name
      <input type="" name="firstName" value="{{user.firstName}}">
    </label>

    <label>
      Last Name
      <input type="" name="lastName" value="{{user.lastName}}">
    </label>


    <label>
      Phone Number
      <input type="tel" name="phone" value="{{user.phone}}">
    </label>

  </div>

  <div class="centered">
    <h3>Select Tags:</h3>
    <p class="subtext">hold ctrl/cmd to select multiple</p>
    <select id="tagSelection" name="tags" multiple size="5">
        {{#each knownTags}}
        <option value="{{this}}" {{#if (listContains this ../userTags)}}selected{{/if}}>
        {{this}}
        </option>
        {{/each}}
    </select>
  </div>

  <div>
    <input id="addTagInput">
    <button type="button" id="addTagButton">Add Tag</button>
  </div>

  <button>Save</button>
</form>

<div id="errorDiv" class="errors centered" hidden>
  <p id="errorMessage"></p>
</div>